Run `uv run python manage.py generate_sample_inputs` to generate local sample resume files in both PDF and DOCX formats under `sample_data/resumes/`.

Use `sample_data/jobs/platform_backend_engineer.txt` as the sample job-description input, or run `uv run python manage.py run_sample_pipeline` to execute the thin-slice demo end to end.
